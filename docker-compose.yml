version: '3.1'

services:
  db:
    image: postgres
    ports:
      - 5432:5432
    environment:
      POSTGRES_USER: cat
      POSTGRES_DB: pets
    volumes:
      - ./server/database/data:/var/lib/postgresql/data